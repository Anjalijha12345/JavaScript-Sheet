const webTechs = [
    'HTML',
    'CSS',
    'JavaScript',
    'React',
    'Redux',
    'Node',
    'MongoDB'
    ]

    const arr=[];
    for(let i=0;i<webTechs.length;i++)
    {
        arr.push(webTechs[i],webTechs[i].length);
    }
    console.log(arr);